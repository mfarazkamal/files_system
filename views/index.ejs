<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="main w-full min-h-screen bg-zinc-900">

        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full rounded-md outline-none px-5 py-2 bg-zinc-800" type="text" name="title"
                    placeholder="Title goes here">
                <textarea class="block mt-2 w-full rounded-md outline-none px-5 py-2 resize-none bg-zinc-800"
                    name="desc" placeholder="Write your task details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 rounded py-2 cursor-pointer" type="submit" value="Create Task">
            </form>
        </div>

        <div class="task flex gap-3 flex-wrap p-10">

            <% if(files.length>0){%>

                <% files.forEach(function(val){%>
                    <div class="rounded  w-72 px-3 py-4 bg-zinc-800">
                        <h1 class="text-white text-2xl tracking-tighter">
                            <%= val %>
                        </h1>
                        <div class="flex w-full justify-between items-center mt-3">

                            <a class="text-blue-500 " href="/file/<%= val %>">Read More</a>
                            <a class="text-blue-500" href="/edit/<%= val %>">Edit File</a>
                        </div>
                    </div>
                    <%})%>


                        <%} else { %>

                            <h3 class="text-zinc-500">No Tasks</h3>

                            <%}%>



        </div>

    </div>

</body>

</html>